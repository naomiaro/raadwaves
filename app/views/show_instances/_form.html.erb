<%= form_for(@show_instance) do |f| %>
  <% if @show_instance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@show_instance.errors.count, "error") %> prohibited this show_instance from being saved:</h2>

      <ul>
      <% @show_instance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
